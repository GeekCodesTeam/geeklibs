<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.geek.swipebacklayout">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:supportsRtl="true">
<!--        android:label="@string/app_namelibswipebacklayout"-->

        <!--        android:theme="@style/AppThemeSwipeBackLayout"-->
        <activity
            android:name=".demo.SwipebacklayoutDemoActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="${applicationId}.hs.act.slbapp.SwipebacklayoutDemoActivity" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>
</manifest>
